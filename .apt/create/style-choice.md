---
confirmed: yes
styleId: apt-skyline-clean
source: apt-preset
visualKeywords: soft blue-gray, cobalt CTA, airy layout, daylight clarity
selectedAt: 2026-09-19T05:11:00.000Z
---

# Style Choice

用户本轮原文含「确认风格」；锁定：**天际清朗 / apt-skyline-clean**。

## designTokens

```css
:root {
  --apt-font-display: "Sora", "Source Han Sans SC", sans-serif;
  --apt-font-body: "Source Sans 3", "Source Han Sans SC", sans-serif;
  --apt-font-mono: "JetBrains Mono", ui-monospace, monospace;
  --apt-bg: #f3f6fa;
  --apt-bg-elevated: #ffffff;
  --apt-surface: #e8eef5;
  --apt-border: #c9d4e3;
  --apt-text: #152033;
  --apt-text-muted: #5b6b82;
  --apt-primary: #1f5eff;
  --apt-primary-hover: #1648d6;
  --apt-accent: #0f9f8a;
  --apt-danger: #d64545;
  --apt-success: #1f9d6a;
  --apt-warning: #c98500;
  --apt-radius-sm: 6px;
  --apt-radius-md: 12px;
  --apt-shadow: 0 10px 30px rgba(21, 32, 51, 0.08);
  --apt-focus-ring: 0 0 0 3px rgba(31, 94, 255, 0.35);
}
```

## 风格约束

- 浅底、钴蓝主按钮；次要操作用线框或文字按钮。
- 工作台信息分区清晰，避免仪表盘式堆叠。
- 禁止大面积紫光/霓虹。
- 颜色一律用 `--apt-*`，禁止页面硬编码新 hex。

## designTokens

```css
:root {
  --apt-font-display: "Sora", "Source Han Sans SC", sans-serif;
  --apt-font-body: "Source Sans 3", "Source Han Sans SC", sans-serif;
  --apt-font-mono: "JetBrains Mono", ui-monospace, monospace;
  --apt-bg: #f3f6fa;
  --apt-bg-elevated: #ffffff;
  --apt-surface: #e8eef5;
  --apt-border: #c9d4e3;
  --apt-text: #152033;
  --apt-text-muted: #5b6b82;
  --apt-primary: #1f5eff;
  --apt-primary-hover: #1648d6;
  --apt-accent: #0f9f8a;
  --apt-danger: #d64545;
  --apt-success: #1f9d6a;
  --apt-warning: #c98500;
  --apt-radius-sm: 6px;
  --apt-radius-md: 12px;
  --apt-shadow: 0 10px 30px rgba(21, 32, 51, 0.08);
  --apt-focus-ring: 0 0 0 3px rgba(31, 94, 255, 0.35);
}
```

## 风格约束

- 浅底、钴蓝主按钮；次要操作用线框或文字按钮。
- 工作台信息分区清晰，避免仪表盘式堆叠。
- 禁止大面积紫光/霓虹。
- 颜色一律用 `--apt-*`，禁止页面硬编码新 hex。
