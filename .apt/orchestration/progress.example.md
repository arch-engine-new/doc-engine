# Orchestration Progress (example)

This file is a FORMAT EXAMPLE. The real ledger is progress.md (same dir).
Status aggregation parses checkbox lines: [x]=done, [ ]=pending, [!]/[~]/[?]=blocked.

- [x] completed task one
- [ ] pending task two
- [!] blocked task three